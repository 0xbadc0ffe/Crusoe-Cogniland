experiment_name: minigrid_doorkey
results_path: results
offline: false

entity: crusoe
project: crusoe_in_cogniland

seed: 42

env_id: minigrid-doorkey
tasks: [0]
task_sampling: round_robin

env:
  gym_id: MiniGrid-DoorKey-6x6-v0
  num_parallel_envs: 128
  num_parallel_envs_eval: 16
  max_steps: 360

# Not used by MiniGrid but trainer reads the block defensively.
reward:
  reach_bonus: 1.0
  step_penalty: 0.0
  shaping_coef: 0.0
  hp_coef: 0.0
  death_penalty: 0.0
  correct_answer_bonus: 0.0
  craft_bonus: 0.0

trainer:
  num_train_frames: 3_000_000
  num_eval_frames: 20_000
  eval_interval_frames: 500_000
  full_eval_enabled: true
  trajectory_viz_interval_frames: null
